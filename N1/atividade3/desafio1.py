#Criar limpar_dados(lista)
def limpar_dados(lista):

    dados_normalizados = 
    #Filtre apenas os usuário com status_ativo == True
    #Use lambda + filter()
    #Normalize os dados com loop
    for nome, email in lista:
    # Nomes em maiuscula (.upper())
        .upper()
    # Email em minuscula (.lower()) 
        .lower()