import pandas as pd

#criar coluna com assign
df_novo = df.assign(
    total = lambda x: x[]
)
#rename the dataframe
#chave == antigo value == novoNome

#aprendizado de maquina
#tarefa de classificação
from sklearn.preprocessing import StandardScaler
import numpy as np

dados
